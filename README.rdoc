== Rails Boilerplate

This boilerplate project was created with minimal configuration for a Ruby on Rails application. It is intended to serve as a template for testing.

=== Usage

* Clone the repo
* <tt>bundle install</tt>
* <tt>rake db:migrate</tt>

To start the server:
<tt>rails s</tt>

Open a web browser to http://localhost:3000
